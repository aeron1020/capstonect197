# Production Configuration Quick Reference\n\n## Environment Variables Overview\n\n### Backend (.env)\n\n```bash\n# Security\nDEBUG=False\nSECRET_KEY=<your-new-secret-key>\nALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,api.yourdomain.com\n\n# Database - PostgreSQL\nDB_ENGINE=django.db.backends.postgresql\nDB_NAME=rmc_system_db\nDB_USER=rmc_user\nDB_PASSWORD=<strong-password>\nDB_HOST=db.example.com\nDB_PORT=5432\n\n# CORS - Restrict to frontend domain\nCORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com\n\n# Email - Gmail example\nEMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend\nEMAIL_HOST=smtp.gmail.com\nEMAIL_PORT=587\nEMAIL_USE_TLS=True\nEMAIL_HOST_USER=your-email@gmail.com\nEMAIL_HOST_PASSWORD=<app-specific-password>\nDEFAULT_FROM_EMAIL=noreply@yourdomain.com\n\n# Security Headers\nSECURE_SSL_REDIRECT=True\nSESSION_COOKIE_SECURE=True\nCSRF_COOKIE_SECURE=True\nSECURE_HSTS_SECONDS=31536000\nSECURE_HSTS_INCLUDE_SUBDOMAINS=True\nSECURE_HSTS_PRELOAD=True\n```\n\n### Frontend (.env.production)\n\n```bash\nNEXT_PUBLIC_API_URL=https://yourdomain.com/api\nNEXT_PUBLIC_APP_URL=https://yourdomain.com\nNEXT_PUBLIC_ENV=production\nNEXT_PUBLIC_API_TIMEOUT=30000\n```\n\n---\n\n## Key Django Settings (Production)\n\n### Before (Development)\n```python\nDEBUG = True\nSECRET_KEY = 'django-insecure-zvxfcx5qo#aye7!o8mu-p8&*6^@8j_v&tau*y3h$)1&6#(6p%('\nALLOWED_HOSTS = []\nCORS_ALLOW_ALL_ORIGINS = True\nDATABASES = {'default': {'ENGINE': 'django.db.backends.sqlite3'}}\n```\n\n### After (Production)\n```python\nDEBUG = os.getenv('DEBUG', 'False').lower() in ('true', '1', 'yes')\nSECRET_KEY = os.getenv('SECRET_KEY', '<random-key>')\nALLOWED_HOSTS = os.getenv('ALLOWED_HOSTS', '').split(',')\nCORS_ALLOWED_ORIGINS = [origin.strip() for origin in \n    os.getenv('CORS_ALLOWED_ORIGINS', '').split(',')]\n\n# PostgreSQL support\nif DB_ENGINE == 'django.db.backends.postgresql':\n    DATABASES = {\n        'default': {\n            'ENGINE': DB_ENGINE,\n            'NAME': os.getenv('DB_NAME'),\n            'USER': os.getenv('DB_USER'),\n            'PASSWORD': os.getenv('DB_PASSWORD'),\n            'HOST': os.getenv('DB_HOST'),\n            'PORT': os.getenv('DB_PORT'),\n            'CONN_MAX_AGE': 600,\n        }\n    }\n```\n\n---\n\n## Key Next.js Configuration (Production)\n\n### Before\n```typescript\nconst nextConfig: NextConfig = {\n  /* config options here */\n};\n```\n\n### After\n```typescript\nconst nextConfig: NextConfig = {\n  compress: true,\n  images: {\n    formats: ['image/avif', 'image/webp'],\n  },\n  headers: async () => [\n    {\n      source: '/:path*',\n      headers: [\n        { key: 'X-Content-Type-Options', value: 'nosniff' },\n        { key: 'X-Frame-Options', value: 'DENY' },\n        { key: 'X-XSS-Protection', value: '1; mode=block' },\n      ],\n    },\n  ],\n  env: {\n    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,\n    NEXT_PUBLIC_APP_URL: process.env.NEXT_PUBLIC_APP_URL,\n  },\n  poweredByHeader: false,\n  swcMinify: true,\n};\n```\n\n---\n\n## API Integration Changes\n\n### Before\n```typescript\nconst api = axios.create({\n  baseURL: 'http://127.0.0.1:8000/api/',\n});\n```\n\n### After\n```typescript\nconst api = axios.create({\n  baseURL: process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:8000/api/',\n  timeout: parseInt(process.env.NEXT_PUBLIC_API_TIMEOUT || '30000', 10),\n});\n\napi.interceptors.response.use(\n  (response) => response,\n  (error) => {\n    if (error.response?.status === 401) {\n      localStorage.removeItem('access_token');\n      window.location.href = '/login';\n    }\n    return Promise.reject(error);\n  }\n);\n```\n\n---\n\n## Gunicorn Configuration\n\n```python\n# gunicorn_config.py\nimport multiprocessing\n\nworkers = multiprocessing.cpu_count() * 2 + 1\nworker_class = \"sync\"\ntimeout = 30\nbind = \"0.0.0.0:8000\"\npidfile = \"/var/run/gunicorn.pid\"\naccesslog = \"/var/log/rmc_system/access.log\"\nerrorlog = \"/var/log/rmc_system/error.log\"\nloglevel = \"info\"\npreload_app = True\nsecure_scheme_headers = {\n    \"PROXY_PROTOCOL\": \"PROXY\",\n    \"X_FORWARDED_PROTOCOL\": \"x-forwarded-proto\",\n}\nforwarded_allow_ips = \"*\"\n```\n\n**Run:**\n```bash\ngunicorn -c gunicorn_config.py rmc_system.wsgi:application\n```\n\n---\n\n## Nginx Configuration Snippet\n\n```nginx\nupstream django_app {\n    server 127.0.0.1:8000;\n}\n\nserver {\n    listen 443 ssl http2;\n    server_name yourdomain.com;\n    \n    ssl_certificate /path/to/certificate.crt;\n    ssl_certificate_key /path/to/private.key;\n    ssl_protocols TLSv1.2 TLSv1.3;\n    ssl_ciphers HIGH:!aNULL:!MD5;\n    \n    # Security Headers\n    add_header Strict-Transport-Security \"max-age=31536000\" always;\n    add_header X-Frame-Options \"DENY\" always;\n    add_header X-Content-Type-Options \"nosniff\" always;\n    \n    location /static/ {\n        alias /path/to/rmc_system/staticfiles/;\n        expires 30d;\n    }\n    \n    location /media/ {\n        alias /path/to/rmc_system/media/;\n    }\n    \n    location / {\n        proxy_pass http://django_app;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n    }\n}\n```\n\n---\n\n## Systemd Service Files\n\n### Backend Service\n```ini\n[Unit]\nDescription=RMC System Django Application\nAfter=network.target postgresql.service\n\n[Service]\nType=notify\nUser=www-data\nGroup=www-data\nWorkingDirectory=/path/to/rmc_system\nEnvironment=\"PATH=/path/to/venv/bin\"\nExecStart=/path/to/venv/bin/gunicorn -c gunicorn_config.py rmc_system.wsgi:application\nRestart=always\n\n[Install]\nWantedBy=multi-user.target\n```\n\n### Frontend Service\n```ini\n[Unit]\nDescription=RMC System Next.js Frontend\nAfter=network.target\n\n[Service]\nType=simple\nUser=www-data\nGroup=www-data\nWorkingDirectory=/path/to/rmc_frontend\nEnvironment=\"NODE_ENV=production\"\nEnvironment=\"PORT=3000\"\nExecStart=/usr/bin/node .next/standalone/server.js\nRestart=always\n\n[Install]\nWantedBy=multi-user.target\n```\n\n---\n\n## Database Setup\n\n### PostgreSQL\n```bash\n# As postgres user\nsudo -u postgres psql\n\nCREATE DATABASE rmc_system_db;\nCREATE USER rmc_user WITH PASSWORD 'secure_password';\nALTER ROLE rmc_user SET client_encoding TO 'utf8';\nALTER ROLE rmc_user SET default_transaction_isolation TO 'read committed';\nALTER ROLE rmc_user SET default_transaction_deferrable TO on;\nALTER ROLE rmc_user SET default_time_zone TO 'UTC';\nGRANT ALL PRIVILEGES ON DATABASE rmc_system_db TO rmc_user;\n\\q\n```\n\n### Django Migrations\n```bash\npython manage.py migrate\npython manage.py createsuperuser\npython manage.py collectstatic --noinput\n```\n\n---\n\n## SSL/TLS Certificate Setup\n\n### Let's Encrypt (Free)\n```bash\nsudo apt install certbot python3-certbot-nginx\nsudo certbot certonly --nginx -d yourdomain.com -d www.yourdomain.com\n\n# Auto-renewal\nsudo systemctl enable certbot.timer\n```\n\n---\n\n## Health Check Endpoint\n\n```python\n# In Django urls.py\npath('api/health/', health_check, name='health_check')\n\n# In views.py\nfrom django.http import JsonResponse\nfrom django.db import connection\n\ndef health_check(request):\n    try:\n        with connection.cursor() as cursor:\n            cursor.execute(\"SELECT 1\")\n        return JsonResponse({'status': 'healthy', 'version': '1.0'})\n    except Exception as e:\n        return JsonResponse(\n            {'status': 'unhealthy', 'error': str(e)}, \n            status=500\n        )\n```\n\n---\n\n## Common Commands\n\n### Start/Stop Services\n```bash\n# Backend\nsudo systemctl start rmc_system\nsudo systemctl stop rmc_system\nsudo systemctl restart rmc_system\nsudo systemctl status rmc_system\n\n# Frontend\nsudo systemctl start rmc_frontend\nsudo systemctl restart rmc_frontend\n\n# View logs\nsudo journalctl -u rmc_system.service -f\nsudo journalctl -u rmc_frontend.service -f\n```\n\n### Database\n```bash\n# Backup\npg_dump -U rmc_user -d rmc_system_db > backup.sql\n\n# Restore\npsql -U rmc_user -d rmc_system_db < backup.sql\n\n# Connect\npsql -U rmc_user -d rmc_system_db\n```\n\n---\n\n## Monitoring\n\n### Check Application Health\n```bash\ncurl https://yourdomain.com/api/health/\n```\n\n### Monitor Disk Usage\n```bash\ndf -h\ndu -sh /path/to/rmc_system/media/\n```\n\n### Monitor Process\n```bash\nps aux | grep gunicorn\nps aux | grep node\n```\n\n---\n\n## Quick Troubleshooting\n\n| Issue | Solution |\n|-------|----------|\n| 502 Bad Gateway | Check Gunicorn: `systemctl status rmc_system` |\n| Static files missing | Run: `python manage.py collectstatic --noinput` |\n| CORS errors | Verify `CORS_ALLOWED_ORIGINS` includes frontend URL |\n| Database connection failed | Check PostgreSQL is running: `sudo systemctl status postgresql` |\n| 404 on API endpoints | Verify Nginx proxy configuration and Django URLs |\n| Slow uploads | Increase `client_max_body_size` in Nginx |\n"